import DarkModeToggle from '../DarkModeToggle/DarkModeToggle';

const Hero = () => {
    return (
        <header>
            <h1>Todo app</h1>
            <DarkModeToggle />
        </header>
    );
};

export default Hero;
